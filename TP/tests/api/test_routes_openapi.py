def test_api_endpoint_exists():
    """Vérifie que la route /triangulate/{id} est accessible."""
    # Pour l'instant, on simule simplement un test échoué
    assert False, "API non implémentée pour le moment"
